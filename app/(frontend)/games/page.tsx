import GamesPage from "@/components/games-page"

export const metadata = {
	title: "Browse Games - RoLinks",
	description: "Browse all available games with private servers. Find your favorite games and discover new ones.",
}

export default function Games() {
	return <GamesPage />
}